package main

import (
	"fmt"
	"math"
)

func hypot(x, y float64) float64 {
	return math.Sqrt(x*x + y*y)
}

func f(i, j, k int, s, t string)
func main() {
	fmt.Println("Hello, World!")
	fmt.Println(hypot(7, 12))
}
